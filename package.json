{
  "name": "ag-agentmemmory-proxy",
  "version": "0.1.0",
  "description": "OpenAI-compatible agy CLI proxy for AgentMemory upstream",
  "type": "module",
  "bin": {
    "ag-agentmemmory-proxy": "dist/cli.js"
  },
  "files": [
    "dist",
    "setup.sh",
    "set-run.sh",
    "agy-clean-wrapper.sh",
    "README.md",
    "README.vi.md"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "verify": "node dist/cli.js verify",
    "status": "node dist/cli.js status",
    "test": "npm run build && node --test test/*.test.mjs",
    "prepack": "npm run build"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.32",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
